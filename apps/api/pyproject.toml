[project]
name = "api"
version = "0.1.0"
description = "API backend (FastAPI + Supabase) para servir resultados electorales."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.25",
    "supabase>=2.0",
    "python-dotenv>=1.0",
]
